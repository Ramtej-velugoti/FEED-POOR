<!DOCTYPE html>
<html>
<head>
    <style>
        body{
	justify-content: center;
	align-items: center;
	min-height: 100vh;
	font-family: 'Jost', sans-serif;
	background: linear-gradient(to bottom, #fffefe, #ecad76, #f2913b);
}
        .donation-form {
            max-width: 400px; /* Reduced maximum width */
            margin: 0 auto;
            color: #ff7700;
            padding: 20px;
            background-color: #f9f9f9;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
        }

        .donation-form h2 {
            text-align: center;
            font-size: 2.0em; /* Reduced font size */
            margin-bottom: 15px; /* Reduced margin */
            margin-top: 5px;
        }

        .donation-form form {
            margin-top: 15px;
        }

        .field {
            margin-bottom: 15px; /* Reduced margin */
        }

        .field label {
            display: block;
            font-weight: bold;
            color: #000000ae;
        }

        .field input,
        .field select,
        .field textarea {
            width: 100%;
            padding: 10px;
            border: 1px solid #d78543;
            border-radius: 4px;
            box-sizing: border-box;
            font-size: 13px;
        }

        .field input:focus,
        .field select:focus,
        .field textarea:focus {
            border-color: #c9782d;
        }

        .field select {
            width: 100%;
        }

        .donation-form button {
            width: 60%;
            background-color: #c74f24;
            color: #fff;
            font-size: 18px;
            padding: 10px;
            border: none;
            margin-left: 70px;
            border-radius: 4px;
            cursor: pointer;
        }

        .donation-form button:hover {
            background-color: #ff4500;
        }
    </style>
</head>
<body>
    <div class="donation-form">
        <h2>Donation Form</h2>
        <form action="/donatesuc" method="POST">
            <div class="field">
                <label for="citizenship">Select your Citizenship</label>
                <select name="citizenship">
                    <option value="indian_citizen">Indian Citizen</option>
                    <option value="foreign_citizen">Foreign Citizen</option>
                </select>
            </div>

            <div class="field">
                <label for="full_name">Donor Details</label>
                <input type="text" name="full_name" placeholder="Full Name*" required>
            </div>

            <div class="field">
                <label for="mobile_number">Mobile Number*</label>
                <input type="tel" name="mobile_number" placeholder="+91-" required>
            </div>

            <div class="field">
                <label for="email">Email Id*</label>
                <input type="email" name="email" placeholder="Enter Email!" required>
            </div>

            <div class="field">
                <label for="pan_card_no">Pan Card No</label>
                <input type="text" name="pan_card_no">
            </div>

            <div class="field">
                <label for="address">Address</label>
                <textarea name="address" placeholder="India" required></textarea>
            </div>

            <div class="field">
                <label for="pincode">Pincode</label>
                <input type="number" name="pincode" required>
            </div>

            <div class="field">
                <label for="state">State</label>
                <input type="text" name="state" required>
            </div>

            <div class="field">
                <label for="city">City</label>
                <input type="text" name="city" required>
            </div>
            <button>Submit</button>
        </form>
    </div>
</body>
</html>
